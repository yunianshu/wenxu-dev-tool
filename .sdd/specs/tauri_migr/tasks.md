# 桌面架构迁移任务

1. 建立 Tauri + Vue 构建和可启动空壳，记录 Electron 基线（规格 6）。
2. 建立受监管的 Node 后台及请求/事件通道，完整接入现有 `window.gitReport` 契约（规格 1、4）。
3. 迁移窗口、对话框、剪贴板、托盘、Git 工作进程、终端和业务服务（规格 1、4）。
4. 迁移 Harness 运行时、独立页面和组件更新（规格 2、3、5）。
5. 完成旧数据和凭据迁移、平台打包与端到端验证，测量体积和性能（规格 5、6）。
6. 验证新路径后切换默认构建/文档，完成项目要求的提交与推送（规格 1-6）。
