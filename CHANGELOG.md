# Personnel PLM · 更新日志

按版本从新到旧排列。标题里的时间是该版本发布的时刻（本地时区，精确到分钟），
取自仓库里对应的发布提交。历史记录依据仓库提交整理，自 1.4.43 起收录；更早版本未整理。

## 未发布

- 部署：新增自动正式发布，选择项目并保存服务器地址与 SSH 凭据即可发布；自动识别多模块技术栈，复用现有 Compose 或使用配置的 AI 生成部署文件，在服务器容器内构建，缺少基础环境时自动准备，构建失败可自动修复重试一次。
- 部署：隔离不同项目的目录、端口、容器与数据卷，移除新项目自动继承其他项目配置；显式复制只带服务器连接。发布使用独立版本镜像，全部容器和业务服务健康才判成功，恢复失败不再误报回滚成功。
- 部署：修复构建产物被默认排除、版本子串误匹配、嵌套发布包找不到、打包失败改动源项目版本等问题；修复 SSH 分段输出导致发布结果标记丢失，增加发布包结构检查。

## 1.4.91 · 2026-09-21 18:04

- 工作台：修复「今日提交」第一次加载分钟级卡顿。仓库扫描是流式发现、逐个入列的，
  工作台原先以列表长度为触发源，每发现一个仓库就用「当时的部分仓库列表」发起一次
  收集；而收集缓存按完整仓库列表签名去重，部分列表每次都算新任务，78 个仓库会放大
  成约 3000 次 git 子进程调用（理想 78 次），把 git 工作进程连同扫描、启动预热一起
  拖入分钟级风暴。现在改为只在仓库列表权威时收集——启动预热完成（或失败放行）且
  当前不在扫描中才发起，与预热参数完全一致、精确命中缓存；设置页手动重新扫描结束后
  同样收敛为一次完整收集。实测本机 78 仓库从分钟级降到 8 秒左右（含一次必要的全量
  预收集，全程有进度显示），git 进程峰值被并发池约束在 16 以内。

## 1.4.90 · 2026-09-21 17:50

- 一键填报：提交记录只在失败时才出现「重新提交」。此前无论成败都显示该按钮，而成功记录
  的禅道工时与汉印条目都已写入并核实通过，重放只会按已有记录 ID 原样覆盖一遍，没有补交
  意义，误点还会白白多打一轮平台请求；现在只有「禅道中断」或「汉印未写入」的记录才提供
  该按钮，主进程也不再接受成功记录的重放（直接提示「该记录已提交成功，无需重新提交」）。
  要调整已提交的工时，仍可改上下班时间后重新生成报告再提交。
- 一键填报：提交记录支持逐条删除。此前本机留痕只增不减（上限 200 条），失败或试验性的
  记录会长期占着面板；现在每条记录带「删除」按钮，确认后只清本机留痕
  （fill-log.json 中该条），禅道/汉印上已写入的工时不受影响，其余记录（含失败条目及其
  重放载荷）保持不变。

## 1.4.89 · 2026-09-21 17:12

- 终端工作台：修复运行 codex 等 TUI 时光标漂移闪烁。ConPTY 会把一帧重绘拆成多个小包
  送达（实测帧内间隔 3~80ms），其中部分包以「光标停在跳板位置 + 显示」结尾，渲染层
  逐包写入时这些帧间过渡态被真实渲染成闪烁漂移的绿块（codex 上游每帧翻转光标可见性，
  Windows Terminal 同样受影响）。现在渲染层按同步输出块（DECSET 2026）边界整帧合并
  批写，光标只呈现帧末落位；按帧边界切批而不再依赖时间上限（时间切分点会锁相落在
  过渡态之后），普通回显走短延迟通道、打字手感不变。E2E 实测闪跳样本 43% → 0，
  按键回显中位延迟 3ms。
- 应用图标更换为 A5「小海狸建造师」，同步窗口、托盘、侧栏与打包图标；生成脚本保留原图完整构图和背景。

## 1.4.88 · 2026-09-21 13:42

- 部署：脚本形态「打包升级版本」闭环。项目打包脚本要求发布说明随版本提供时（如 Vantage
  package.sh：docs/release-notes-<版本>.md 缺失即中止打包），发布前工具自动探测该约定
  （项目根或 docs/ 下已有 release-notes-*.md 即认定），目标版本文件缺失就依据「上次发布
  之后的提交」自动生成初稿再执行打包，不必先回项目手工准备；文件在工作区未提交，日志会
  提醒润色后随代码提交。既有说明一律不覆盖；无该约定的项目零打扰；部署设置新增「缺失发布
  说明时自动生成初稿」开关（默认开）。

## 1.4.87 · 2026-09-21 11:17

- 终端工作台：Ctrl+Enter / Shift+Enter 现在插入换行，不再把消息直接提交出去（claude
  等 TUI 场景）。根因：xterm 的 Enter 分支只区分 Alt，这两组修饰回车也被翻成 \r，
  claude 解析 \r 即提交；改发 LF（\n），即 claude 官方 Ctrl+J 的换行序列，任何 TUI
  无需配置。Alt+Enter 保持 xterm 默认 ESC+\r（同为 claude 认可的换行键，/terminal-setup
  给 VSCode 配的就是该序列）。顺带补齐 Ctrl+Shift+V 粘贴（对齐 Windows Terminal）。
  中文输入法选词回车（isComposing / keyCode 229）不受影响；裸 Enter、Ctrl+C、Ctrl+J、
  Shift+Tab 等原有行为经真实链路回归无变化。

## 1.4.86 · 2026-09-20 18:03

- 终端工作台：在终端里 cd 之后，窗格标题栏的路径现在实时跟随。实现：PowerShell 家族
  启动参数注入一个只包 prompt 的终端集成钩子（-EncodedCommand 下发，保留用户原有提示符
  与 profile，不执行任何用户命令），每次出提示符把当前目录以 OSC 9;9 发给主进程；主进程
  从输出流里解析 OSC 9;9 / OSC 7（跨数据块拼接、容错脏序列），更新会话目录并广播给渲染层。
  会话目录（sessionCwd）与窗格绑定的项目目录（cwd）分开存：切页回来按 paneId 认领会话，
  不再按目录匹配把 cd 过的会话误判成孤儿关掉；cmd 窗格没有上报机制，标题保持项目目录。

## 1.4.85 · 2026-09-20 15:48

- 终端工作台：分屏方式分段控件的上下边框看不见。Element Plus 2.9 给每段单独画
  outline（相邻段两条错开叠成 2px 深竖线，上下只有单条 1px 浅灰），上一版加深颜色
  到 #b9c0ca 仍不够。改为整组 1px 深灰外框（#909399）+ 段间内侧分隔线，四边同粗
  同色；外框必须画在控件自身 border box 内——顶栏插槽容器是 overflow:auto hidden，
  画在组外的 box-shadow/outline 会被纵向裁掉（像素级实测翻车过）。outline 移除后
  补回键盘焦点指示环。

## 1.4.84 · 2026-09-20 15:15

- 产品更名：应用显示名由「文须项目管理」改为「Personnel PLM」，覆盖窗口标题、侧栏品牌名、
  托盘提示、关闭确认框、设置页「关于」与 AI 助手人设，以及打包产物 exe 名、NSIS 安装包名
  和桌面快捷方式名。侧栏方形品牌标记同步由「项」改为「P」（收起侧栏时只余此标记）。
- 不随更名改动的部分（与上次更名一致，有意保持不变）：用户数据目录 `%APPDATA%\dev-project-manager`、
  安装目录 `%LOCALAPPDATA%\Programs\dev-project-manager`、appId 与 GitHub 发布仓库，
  因此既有项目数据、配置与更新链路不受影响。
- 本地更新脚本 `install-local.cjs` 适配本次更名：`LEGACY_PRODUCT_NAMES` 追加「文须项目管理」，
  沿用既有兼容逻辑——关闭旧名进程（避免占用被同步删除的旧 exe）、按旧显示名匹配残留的
  「所有用户」安装、更新后清理指向已删 exe 的旧名桌面快捷方式。
- 修复关闭询问框「取消」不生效：点右上角 × 或按 ESC 取消后，窗口仍会在 8 秒后被最小化到
  托盘。根因：主进程为防止「渲染层卡死不响应」设了 8s 兜底（超时按默认语义最小化），
  而渲染层在 ×/ESC 时并不回传结果，兜底定时器因此永不清除。现 ×/ESC 回传 `cancel`，
  主进程只清兜底、不对窗口做任何动作。
- 更名验证 E2E 补强：原「取消后窗口保留」断言只查页面 DOM 是否还在，而窗口被隐藏到托盘时
  DOM 依然存在，属假阳性（该假阳性正是上面这个缺陷长期未被发现的原因）。现改用 OS 层
  窗口标题可读性判定，并在点击关闭按钮之前先采样一次标题、规避与 8s 兜底的时序竞态。
- 应用图标换为品牌新图标（深绿底 + 剪贴板勾选 + 上升箭头与柱状图），覆盖 exe、NSIS 安装包、
  便携版、托盘与窗口。设计稿为不透明白底，新增 `scripts/gen-icons.cjs` 自动抠出透明圆角，
  并对圆角外做颜色扩散——否则降采样时边缘会混入背景色，在深色任务栏上露出白边——
  再生成 1024 主图与 256/128/64/48/32/24/16 七档 ICO；`scripts/verify-icons.cjs`
  校验透明边缘、白边渗入与各档位有效性，可经 `npm run gen:icons` / `npm run verify:icons`
  复现与自检。背景判定按「高亮度 + 低饱和度」而非「纯白」：3D 设计稿的图标外围常带
  浅灰投影/光晕，按纯白泛洪会在圆角外留下灰白残留（实测过渡带比内侧白 +129，改后 +19）。
- 小尺寸取舍（已知）：托盘与任务栏按 16x16 呈现时，图标细节不可辨，仅余深绿圆角底、
  白色剪贴板与浅绿高光；上一版为极简绿色分支符号，该尺寸下轮廓更清晰。经确认维持新图标。
- 工作台改为「今日工作台」：原页面的「项目能力」三行就是侧栏菜单、「最近记录」是活动报告页
  与部署页历史的前 5 条，指标带四个数字里三个是配置状态（项目总数 / 已关联目录 / 已配置部署），
  配置完就不再变化。现改为应用级总览——今日状态带（今日提交 / 今日报告 / 今日工时 / 最近部署）
  加「需要处理」清单（今日缺报告、今日缺工时、填报未确认的异常，无事时整块不出现）；
  去掉单项目上下文（顶栏项目切换器、「查看项目资料」按钮与「当前项目」卡片）。
- 终端工作台新增字号调节：原先硬编码 13px，而同一块屏幕上各人看着舒服的字号差别不小。
  入口在「设置 → 界面」（11–22px），改动即时作用于全部窗格并重算行列数——字号变了字符
  尺寸也变，需把新的行列数同步给 pty，否则换行会错位；经 ui-prefs.json 持久化。
- 修正界面偏好的写入方式：主进程 ui-prefs.js 以**整份偏好**为单位 normalize + 落盘，
  原先各调用点只传自己关心的字段（`uiPrefsSave({ sidebarCollapsed })`），加第二个偏好后
  必然互相冲掉。现统一走 `utils/ui-prefs.js` 的 `saveUiPrefs()`，从 store 取完整状态写入。
- 界面精修（对照各页真实截图逐页评审后的定点修复，保持「白面 + 细线」语言、不动布局结构）：
  收紧密度（行高 44→40、卡片体内边距 20→16、内容区上下留白），表格补极浅隔行底色与明确
  行高；**表格内的危险操作默认降为次要按钮、悬停才变红**——红色是警示色，逐行出现会让它
  贬值（活动报告一屏 19 个红按钮，整张表泛红）；扩展管理卡片不再重复分区标题「技能 Skills」
  （它原先在每张卡片上重复一次，却是卡片里最显眼的字），主数据让给「启用 n / m」；
  设置页 AI 表单控件宽度从内联的 240/400/440/260 四种统一到 400px；一键填报工具条按语义
  分组；部署页阶段 chips 改等宽网格、发布日志空态由 300px 纯黑块改浅色面板；
  清理 styles.css 里逐字重复两份的规则块与随之失效的样式。
- 设置页新增「界面」分区，承载全局外观与行为偏好：「窗口关闭行为」由「应用信息」迁入，
  后者回归纯「关于」；样式上把只服务关闭行为的 `.close-behavior` / `.cb-head` 提升为通用的
  `.settings-item` / `.settings-item-head` 供多个设置项复用。
- 修复 AI 部署助手结果区被压扁：`.ai-deploy` 同时设了 `max-height` 与 `overflow: auto`，
  子项却保留默认的 `flex-shrink: 1`，内容超高时浏览器先压缩子项而不是滚动，每个卡片都被
  压到几十像素高。改为子项 `flex-shrink: 0` 走整体滚动，并把操作栏吸顶。
- 终端工作台工具栏：分屏方式由文字改图标（方框 + 分割线示意怎么切格子，hover 出文字，
  并保留 aria-label——hover 提示对键盘与读屏用户不可达）；五个控件统一高度 36px
  （原先主按钮受全局 `min-height: 40px` 影响是 40px、其余是 `size="small"` 的 28px，
  并排高矮不一）；分段控件的**上下边框**此前看不见——Element Plus 2.9 用 outline（不是
  border）画边框，相邻段共享的竖线是两条 outline 错开叠成的 2px，上下只有单条 1px 浅灰，
  在白底上消失，加深 outline 颜色后四边视觉重量相当。

## 1.4.83 · 2026-09-20 10:07

- 产品更名：应用显示名由「开发项目管理 / 个人项目管理」统一为「文须项目管理」，
  覆盖窗口标题、侧栏品牌名、托盘提示、关闭确认框、设置页「关于」与 AI 助手人设，
  以及打包产物 exe 名、NSIS 安装包名和桌面快捷方式名。
- 不随更名改动的部分（有意保持不变）：用户数据目录 `%APPDATA%\dev-project-manager`、
  安装目录 `%LOCALAPPDATA%\Programs\dev-project-manager`、appId 与 GitHub 发布仓库，
  因此既有项目数据、配置与更新链路不受影响。
- 本地更新脚本 `install-local.cjs` 适配更名：关闭旧名进程（避免占用被同步删除的旧 exe）、
  按旧显示名匹配残留的「所有用户」安装、更新后清理指向已删 exe 的旧名桌面快捷方式。

## 1.4.82 · 2026-09-18 18:25

- 修复一键填报提交中断只成功首项（用户报告：当天 4 项只成功 1 项，事后无法回溯）。
  根因：禅道 recordEstimate 实际写入成功但响应体非 JSON 时，旧实现按「无法确认」
  抛错中止，首个任务之后的全部任务与汉印条目都不会提交。现响应异常时回查当日
  工时记录，与本次提交逐行匹配则按已写入继续，不再中断后续提交。
- 提交留痕升级（fill-log.json）：分项记录每个禅道任务（工时/是否回查核实/更新或
  新增）与汉印结果；失败时记录断点环节、响应原文与完整提交载荷；失败提示标明
  已写入进度（如「已写入 1/2 个禅道任务」）。留痕时间带毫秒，杜绝同秒撞键。
- 一键填报页新增「提交记录」面板：列出最近提交的状态与分项计数，失败条目可
  一键「重新提交」——按留痕载荷重放，已有记录按 ID 复用更新覆盖，不会重复写入。
- 预览（dryRun）不再写入提交日志。新增 fill-resubmit-e2e E2E（R1–R4）覆盖
  「响应异常回查继续 / 部分失败留痕 / 面板展示 / 重新提交补全」全链路。

## 1.4.81 · 2026-09-18 14:05

- 活动报告明细的项目卡片支持点击收缩：点项目名所在行即收起/展开该项目的提交
  列表（折叠箭头指示方向，带过渡动画），行内「复制」按钮不触发收缩；多个项目
  时明细工具条出现「全部收起/全部展开」一键切换。重新生成报告后折叠状态重置
  为全部展开。E2E 补充收缩/展开/复制区不触发/全部收起四项断言。

## 1.4.80 · 2026-09-18 13:49

- 修复活动报告只包含单个项目（用户报告：活动报告应生成全部项目，实际只出一个项目）。
  根因是报告页按顶栏「当前项目」过滤活动源，而应用启动会自动选中第一个项目、顶栏
  选择器又没有「全部项目」选项，报告因此永远只覆盖一个项目。现在活动报告固定汇总
  扫描到的全部仓库：活动源数、明细分组、统计图表与导出的 Markdown 均为全量口径，
  历史记录标题统一为「全部项目日报/周报/月报」；顶栏切换项目不再影响报告页。AI 助手
  页按单项目刷新 Git 活动后回到报告页，旧数据仍会归零并提示重新收集（范围一致性
  兜底保留）。
- 重写 report-project-switch E2E 为 report-all-projects E2E（沙箱两个真实 Git 仓库 +
  两个项目）：断言活动源数等于全部仓库数、当前项目为 A 时明细同时含 A/B 提交、历史
  标题为「全部项目」口径、切项目后报告不变、AI 页单项目刷新后报告页归零并提示。

## 1.4.79 · 2026-09-18 12:25

- 修复终端工作台无法粘贴（用户报告：终端里跑 kimi --auto 后粘贴内容毫无反应；
  实测普通 shell 同样粘不进去）。根因是两层叠加：应用移除了菜单栏，此环境下的
  Electron 不再派发浏览器原生 paste 事件，xterm 会把 Ctrl+V 翻成 0x16（^V 控制
  字符）直接发给终端程序——粘贴文本永远到不了对端。现在终端自己实现粘贴：
  Ctrl/Cmd+V 与 Shift+Insert（对齐 Windows Terminal 习惯键）读系统剪贴板后交给
  xterm 的 paste 接口，与原生粘贴同链路，按程序请求的 bracketed paste 语义包裹
  （kimi 等 TUI 依赖该语义识别粘贴），多行内容照常归一换行；剪贴板为空时不向
  终端发送任何字节。拦截按键时显式 preventDefault，避免浏览器默认动作再触发
  一次原生粘贴导致内容发两份。
- 补充剪贴板读取 IPC（clipboard:read）：原先只暴露写入（选区复制），终端粘贴
  需要读系统剪贴板。
- 新增 terminal-paste E2E（真实 Electron + CDP 真实键盘 + OS 剪贴板 + pty 侧
  字节落盘断言）：覆盖普通程序粘贴、bracketed paste 模式粘贴（模拟 kimi TUI）、
  Shift+Insert、空剪贴板零发送四类场景；另经真实 kimi --auto 会话验证粘贴文本
  进入其输入框，复制功能 E2E 回归通过。

## 1.4.78 · 2026-09-18 01:07

- 修复一键填报汉印日报连续失败且无法回溯的问题（9-16、9-17 两晚「禅道已提交、
  汉印没写进去」）：汉印条目构造依赖的任务字典链路里任何一环瞬时失败都会被静默
  吞掉，分组以残缺甚至空数组「伪装成功」，条目落空后汉印整段被跳过——确认弹窗、
  提交结果只字不提，用户只看到绿色成功提示。现在字典获取失败如实报错不再吞掉、
  空分组不进 60 秒缓存（重试会真实重取）；确认弹窗与提交提示在「本次没有可写入
  汉印的占比条目」时明确警示；提交无论成败都在 userData/fill-log.json 留痕
  （近 500 条），失败原因可回溯。
- 汉印 add 接口补上 token 过期（code=-2）自动重登重试：此前只有 GET 有该兜底，
  提交瞬间 token 过期会以近乎空白的「汉印提交失败」告终。仅对 -2 重试（服务端
  未受理，重试不会重复写入），超时等「可能已写入」的失败不重试，避免重复记录。
- 校准 fill-default-endpoints E2E 一条过时断言：地址行的说明文案已按界面文案
  清理要求移除，断言仍要求该文案存在；现改为断言描述性文案不回潮（默认地址本身
  由值断言覆盖）。

## 1.4.77 · 2026-09-17 13:53

- 修复终端工作台添加窗格后显示错乱（行重叠、内容残留、文字挤在半边）：新窗格
  的会话以 xterm 默认 80×24 创建，而首次尺寸适配（60ms 防抖）跑在会话 id 落位
  之前，那次尺寸上报因 id 为空被丢弃；之后视图尺寸不再变化就永远没人补报，PTY
  从此停在 80×24——TUI 程序按旧列宽行数重绘全部错位。现在创建会话前先按当前
  视图尺寸适配，并在会话 id 落位后必补一次尺寸上报，新建/附加/重开三条路都不
  再漏报。
- 终端画布不再被裁边：尺寸适配原先把窗格容器的内边距也算进可用空间，xterm
  画布比可视区大出一圈，最右几列与最下一行被裁掉。现在 xterm 挂到无内边距的
  内层适配层上，行列数按真实可视区域计算。
- 校准终端工作台交互 E2E 里一条过时的顶栏断言：顶栏插槽容器调整为「保持可拖
  拽、仅控件退出拖拽区」后，该断言仍按旧方案要求整个插槽为 no-drag，导致基线
  也失败。现改为断言按钮退出拖拽区（点得动）、标题保持可拖拽（能拖动窗口）。

## 1.4.76 · 2026-09-16 18:05

- 修复一键填报「绑定禅道任务」选不到已完成任务的问题：任务候选此前只取「指派给我」
  入口，而该入口只列进行中与我关闭/取消的任务，已完成的一条都没有——完成后想补填
  工时、或从他人名下接手完成（指派人不是自己，如 #6813）的任务根本选不出来。现在
  并行取「我完成的」入口并按 id 合并去重，已完成任务照旧只列近一个月内完成的；
  该入口取不到时不影响进行中列表。

## 1.4.75 · 2026-09-16 15:55

- 终端工作台修好复制：此前在 PowerShell 窗格里选中文字按 Ctrl+C 不会复制——
  按键被原样当 `^C` 发给终端，剪贴板不变，还会顺带中断正在跑的命令。现在对齐
  Windows Terminal 的约定：有选区时 Ctrl+C 只复制不中断，无选区时照常发 `^C`；
  另支持 Ctrl+Insert 与 Ctrl+Shift+C 复制。
- 修复 `install:local`（本地更新脚本）自 1.4.74 起无法运行的问题：引用
  `prune-releases.cjs` 时漏写扩展名，Node 解析不到模块。

## 1.4.74 · 2026-09-16 15:04

- 侧边栏可以收起了：顶部品牌行右侧新增展开/收缩按钮，收起后侧栏从 216px 收到 64px，
  只留图标——鼠标悬停图标会弹出对应文案，分组标题变成一条细分隔线，底部保留在线
  状态点与版本号。收起的宽度让给内容区，窄窗口下更好用。
- 收起状态会记住：下次打开应用仍是收起的样子（存于用户数据目录的 `ui-prefs.json`，
  与设置项分开存放）。

## 1.4.73 · 2026-09-16 09:50

- 修复更新 Harness 后服务起不来的问题：更新/停止时强杀 dsh 进程可能恰逢其在
  重建模块链接（持 `~/.dsh/profiles/node_modules.lock` 写锁），孤儿锁残留会让之后
  每次启动都在锁等待上超时失败。现在每次启动前检查该锁，持有者进程已退出时自动
  清理（存活则不动，交回 dsh 正常的锁等待）。

## 1.4.72 · 2026-09-15 23:19

- 更新日志里每个版本的发布时间精确到分钟（原先只有日期）。历史版本的时间按各自的
  发布提交回填，没有独立发布提交的按版本号写进 package.json 的那次提交回填。

## 1.4.71 · 2026-09-15 23:15

- 窗口最上面那一栏的空白处，现在整条都能按住拖动窗口了。之前只有最左、最右两条
  很窄的缝能拖，标题右边那一大片空白按下去毫无反应，窗口几乎挪不动。现在标题旁边、
  标题与右侧最小化/最大化/关闭按钮之间的空白，按住都能拖窗口；双击这些空白处也能最大化。

## 1.4.70 · 2026-09-15 22:03

- 所有页面的标题与操作按钮都移到了窗口最上面那一栏，页面里不再各占一条：
  工作台、项目、AI 助手、活动报告、一键填报、部署、扩展管理、设置
  （终端工作台与 DeepSeek Harness 上一版已经是这样）。页面内容相应上移，一屏能多看一些。
- 最上面那一栏不再有独立的「当前项目」下拉。需要项目的页面把项目名直接挂在标题旁边，
  显示成「部署 · 自测项目 1」，点一下就能换项目；与项目无关的页面（设置、扩展管理、
  一键填报、终端工作台、Harness）不再显示它。活动报告页按你选的没放项目控件，
  仍按当前项目出报告（没选项目就汇总全部）。
- 工作台标题不再被项目名顶替：固定显示「工作台」，项目名放在后面的下拉里。
- 项目页、AI 助手、部署页原来「请从顶部选择项目」的提示改掉了：现在到「项目」页选中
  一个项目即可（项目页自带列表，不需要额外入口）。
- 各页面顶部留白改由内容区统一给出（原先靠那条被移走的标题栏撑着，移走后不加就会贴边）。

## 1.4.69 · 2026-09-15 20:05

- 扩展管理页和设置页的内容不再贴着窗口左右边缘：两页的内容统一留出和标题一样的
  左右间距。扩展管理页的技能、插件分区改成和其他页面一致的整幅分区带（标题一行、
  卡片一行），设置页顶部的分区切换条也不再顶在最左边。
- 扩展管理二级页（某个平台下的技能/插件列表）的返回按钮、打开目录、刷新按钮和
  子项卡片，以及「未检测到平台目录」这类提示条，都跟着同一套左右留白。

## 1.4.68 · 2026-09-15 19:48

- 活动报告页顶部那条只写着「活动报告」的标题栏去掉了：它下面没有任何按钮，
  侧栏也已经标明当前在哪个页面，这条标题纯属重复占地方。去掉后周期选择、
  生成报告这些工具和报告列表整体上移，一屏能多看一些内容。
- 去掉时顺手把页面留白补回了原来的样子，内容不会贴着窗口边缘。

## 1.4.67 · 2026-09-15 19:38

- 终端工作台窗口四周的留白改成一样宽：原来左右各 32 像素、上下只有 18～20 像素，
  整个界面看着偏上、左右偏空。现在四边统一，左右让出来的空间给了终端，窗口都宽了一点。
- DeepSeek Harness 页换成同一套留白，两个工具页看起来一致。

## 1.4.66 · 2026-09-15 19:19

- 终端工作台、DeepSeek Harness 两个页面的标题与操作按钮，移到了窗口最上面那一栏。
  这两页跟「当前项目」没什么关系，上面那栏以前基本空着，现在正好放它们自己的
  标题和按钮（终端分屏、添加窗口；Harness 的刷新、重启、全屏等）。
- 顺带省下了一整条标题栏的高度：终端窗口更高了（四宫格时每个窗口多出约 60 像素），
  Harness 的界面也变得更大。
- 其他页面（项目、部署、报告、设置等）保持不变，最上面那栏照旧显示「当前项目」。

## 1.4.65 · 2026-09-15 18:55

- 修复终端工作台有四个窗口时，选「上下」只显示两个窗口的问题：另外两个被挤成了
  两三像素高（看着像消失了），现在会老老实实竖排成四行。
- 「左右」「三宫格」在窗口数多于一屏格子时也不会再挤没多余的窗口：
  这类分屏按钮现在表示排列方向，格子不够就自动往下加行。
- 分屏比例改成按实际行列数记录（竖排四行就存四个比例），关掉软件再打开能原样还原。
- 已经有四个窗口时，「添加窗格」按钮不再可点（最多同屏四个）。

## 1.4.64 · 2026-09-15 18:02

- 修复终端工作台里除左上角那一个以外，其余命令行窗口点不动、也打不了字的问题：
  分隔条之前铺满了整个窗口区域，把鼠标操作（点击、选字、滚动、标题栏按钮）全挡掉了，
  拖动分隔条调整窗口大小也一起失效。
- 拖动分隔条后窗口宽度会多出一截空白的问题一并修好，现在分隔条正好压在两格中间。
- 关掉窗口、切换命令行程序这类改动，切到其他页面后马上关闭软件也不会丢：切页时就立刻记下来。
- 万一某个窗口对应的项目暂时不可用，恢复布局时会跳过它并给出提示，但不会把它从记录里抹掉，
  项目恢复可用后下次打开还能照原样还原。

## 1.4.63 · 2026-09-15 16:52

- 软件的下载与自动更新地址换到了更名后的 GitHub 仓库，旧地址不再接收更新。
- 新增「终端工作台」：在软件里就能打开真正的命令行窗口，一个窗口对应一个项目，
  最多可以同时并排 4 个窗口（四个项目一起盯着），窗口大小能拖动调整。
- 切到别的页面时命令行窗口不会关闭：命令继续在后台运行，最近的输出内容也留着，
  切回来还能接着看（最多保留约 25 万字）。
- 窗口布局会自动记住：窗口数量与顺序、各自对应的项目、用的命令行程序和分屏比例，
  下次打开软件自动恢复成上次的样子。
- 命令行程序自动挑选：优先用 PowerShell 7，没有就用 Windows PowerShell 5.1，
  再没有就用命令提示符；每个窗口都能单独切换。新开窗口只进入项目目录，不会自动执行任何命令。
- 项目页的「项目能力」新增「终端工作台」入口，可以直接为这个项目打开命令行窗口；
  原来的「外部 PowerShell」入口继续保留。
- 关闭软件时会一并结束所有命令行窗口和它们启动的程序，不会在系统里留下残留进程。

## 1.4.60 · 2026-09-15 10:57

- 补全更新日志 1.4.51 ～ 1.4.59 的版本记录。

## 1.4.59 · 2026-09-15 10:44

- 内置 DeepSeek Harness 支持更新监听与应用内热更新：自动检查 npm 源上的新版本，
  有更新时侧栏菜单出现「新版本」角标并弹提示，可在应用内直接下载安装新版本
  （目标机器仍无需安装 Node/npm），更新期间服务自动重启，失败保留原版本。
- Harness 服务信息条显示当前 dsh 版本号。

## 1.4.58 · 2026-09-14 19:16

- 一键填报绑定禅道任务时，可选择的任务范围扩大到近一个月内完成的任务；
  列表中进行中的任务在上、已完成的在下。

## 1.4.57 · 2026-09-14 18:32

- 内容区整体重设计（分区带布局），并清理界面文案与排版。
- 修复生成报告后修改上下班时间不立即重算工时的问题。

## 1.4.56 · 2026-09-12 21:25

- 一键填报的上班时间选择范围放宽，最迟可到 21:00。

## 1.4.55 · 2026-09-12 21:18

- 修复一键填报页隐藏当前项目后，窗口控制按钮仍固定在右侧的问题。

## 1.4.54 · 2026-09-12 19:46

- 一键填报中汉印工时写入失败时，不再掩盖禅道填报成功的结果。

## 1.4.53 · 2026-09-12 01:07

- 一键填报页隐藏顶栏的当前项目切换器（填报与全局项目切换无关）。

## 1.4.52 · 2026-09-11 22:37

- 修复绑定的禅道任务已完成、不在我的任务列表时取不到剩余工时的问题，
  改为回退到任务详情读取。

## 1.4.51 · 2026-09-11 12:05

- 新增应用内更新日志，可从侧栏底部版本入口或「设置 → 应用信息」打开，支持离线查看。
- 部署成功后自动创建 Git 标签，并按版本范围生成更新内容。
- 加强 AI 部署助手的文件写入边界与方案安全校验。
- 修复一键填报的工时覆盖日期及提交失败处理，重复填报不再重复扣减剩余工时。

## 1.4.50 · 2026-09-11 06:24

- 修复 AI 部署助手点击「套用到部署配置」确认后无反应的问题。

## 1.4.49 · 2026-09-11 00:48

- 发布历史支持根据 Git 提交生成通俗易懂的中文更新内容。

## 1.4.48 · 2026-09-11 00:00

- 修复主进程语法错误导致应用启动时弹出 JavaScript 错误的问题。

## 1.4.47 · 2026-09-10 23:43

- 新增 AI 部署助手，支持首次部署体检、生成部署文件和数据同步建议。
- 为脚本部署补充 CURRENT 版本识别兜底。

## 1.4.46 · 2026-09-10 22:37

- 数据库备份配置按部署环境独立保存，兼容迁移旧配置。

## 1.4.45 · 2026-09-10 22:27

- 一键填报时，当天有提交的项目至少分配 0.5 小时工时，避免少量提交的项目被遗漏。

## 1.4.44 · 2026-09-10 21:49

- 修复切换项目后活动报告仍显示上一个项目提交的问题。

## 1.4.43 · 2026-09-10 21:39

- 一键填报先生成当天全部项目的提交明细，再按勾选项目重新分配工时。
- 汉印工时填报不再写入占比为 0% 的条目。
